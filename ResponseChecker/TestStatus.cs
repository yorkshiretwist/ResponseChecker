namespace ResponseChecker
{
    /// <summary>
    /// Enumeration of the different statuses a test can have
    /// </summary>
    public enum TestStatus
    {
        NotYetRun,
        InProgress,
        Failed,
        Succeeded,
        ExceptionEncountered
    }
}
